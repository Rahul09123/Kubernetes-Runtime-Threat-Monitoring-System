FROM golang:1.22-alpine AS builder
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /bin/analyzer ./cmd/analyzer

FROM gcr.io/distroless/static-debian12
COPY --from=builder /bin/analyzer /analyzer
EXPOSE 8081
ENTRYPOINT ["/analyzer"]
